# Implement From Approved Spec

You must implement only what is defined in the approved spec.

Do not invent features.
Do not change unrelated files.
Do not rewrite large files without approval.
Do not change public contracts unless the spec requires it.

## Required reading

1. Read the repository constitution:
   - `constitution.back.md` if this is the backend repository.
   - `constitution.front.md` if this is the frontend repository.
2. Read `AGENTS.md`.
3. Read the approved spec:

`<spec-path>`

## Before implementation

First provide a short plan.

The plan must include:

- Feature being implemented
- Files or layers affected
- Database changes if any
- Tests needed
- Risks or unclear points

Do not implement until the plan is approved.

## Implementation rules

After approval:

- Implement only the approved spec.
- Create new files only inside the approved scope.
- Keep the code simple.
- Follow existing naming and architecture.
- Add or update relevant tests.
- Do not add dependencies unless explicitly approved.

## Final response

After implementation, report:

- Files changed
- Tests added or updated
- Commands run
- Anything not completed